{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build yuv_read",
            "type": "shell",
            "command": "gcc",
            "args": [
                "yuv_read.c"
            ],
            "options": {
                "cwd": "${workspaceFolder}/HW1/"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}